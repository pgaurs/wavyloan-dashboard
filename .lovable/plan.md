Replace the subtitle text under the "Wavyloan" wordmark in the top nav.

- File: `src/routes/index.tsx`, inside `TopNav`
- Change: `Deloitte OpenAI Technology Practice` → `Reimagining Loan Processing`
- No other changes (position stays left of the nav as before; the user's "top right" likely refers to the header area — the existing layout already keeps the subtitle directly below the logo).